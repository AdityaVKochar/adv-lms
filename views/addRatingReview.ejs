<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Rating and Review</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <header class="bg-blue-600 text-white p-4 flex justify-between items-center">
        <h1 class="text-2xl">Library Management System</h1>
        <form class="flex items-center" action="/search" method="GET">
            <input type="text" placeholder="Search books..." name="query" class="p-2 rounded-l border-0  text-gray-800">
            <input type="submit" value="Search" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-r">
        </form>
        <button class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded ml-2" onclick="window.location.replace('/dashboard')">Home</button>
        <button class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" onclick="logout()">Logout</button>
    </header>
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md mx-auto my-5">
        <h1 class="text-2xl font-bold mb-6 text-center">Add Rating and Review for Book ID: <%= book_id %></h1>
        <form action="/submitRatingReview" method="POST">
            <input type="hidden" name="book_id" value="<%= book_id %>">
            <div class="mb-4">
                <label for="rating" class="block text-gray-700">Rating (1-5):</label>
                <input type="number" id="rating" name="rating" min="1" max="5" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" required>
            </div>
            <div class="mb-4">
                <label for="review" class="block text-gray-700">Review:</label>
                <textarea id="review" name="review" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"></textarea>
            </div>
            <button type="submit" class="w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">Submit</button>
        </form>
    </div>
</body>
</html>