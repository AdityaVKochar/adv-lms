<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <header class="bg-blue-600 text-white p-4 flex justify-between items-center">
        <h1 class="text-2xl">Library Management System</h1>
        <form class="flex items-center" action="/search" method="GET">
            <input type="text" placeholder="Search books..." name="query" class="p-2 rounded-l border-0  text-gray-800">
            <input type="submit" value="Search" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-r">
        </form>
        <button class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded ml-2" onclick="window.location.replace('/dashboard')">Home</button>
        <button class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" onclick="logout()">Logout</button>
    </header>
    <main class="p-4">
        <section class="bg-white p-4 rounded shadow-md mb-4">
            <h2 class="text-xl font-bold mb-2">Search Results</h2>
            <div class="book-list flex flex-wrap mb-4">
                <% books.forEach( book => { %>
                <div class="w-full md:w-1/2 p-2">
                    <p><strong>Title:</strong> <span id="book-title"><%= book.title %></span></p>
                    <p><strong>Author:</strong> <span id="book-author"><%= book.author %></span></p>
                    <p><strong>ISBN:</strong> <span id="book-isbn"><%= book.isbn %></span></p>
                    <p><strong>Genre:</strong> <span id="book-genre"><%= book.genre %></span></p>
                    <a href="/book/<%= book.book_id %>" class="text-blue-500 hover:underline">View Book</a>
                </div>
                <% }); %>
            </div>
        </section>
    </main>
    <script>
        function logout() {
            fetch('/signout', {method: 'POST'});
            window.location.replace('/signin');
        }
    </script>
</body>
</html>
